{
  "name": "moonriver",
  "version": "1.0.0",
  "description": "Indexer for moonriver staking",
  "main": "dist/index.js",
  "scripts": {
    "build": "tsc -b",
    "prepack": "rm -rf dist && npm build",
    "test": "jest",
    "codegen": "./node_modules/.bin/subql codegen"
  },
  "homepage": "https://github.com/subquery/subql-starter",
  "repository": "github:subquery/subql-starter",
  "files": [
    "dist",
    "schema.graphql",
    "project.yaml"
  ],
  "author": "Hao",
  "license": "Apache-2.0",
  "devDependencies": {
    "@polkadot/api": "^6.9.1",
    "@subql/cli": "latest",
    "typescript": "^4.5.2",
    "@subql/types": "latest"
  },
  "dependencies": {},
  "resolutions": {
    "@polkadot/util-crypto": "7.8.1",
    "@polkadot/api-derive": "6.8.2-18"
  }
}